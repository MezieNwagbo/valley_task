type PaginationType = {
  start: number;
  totalItems: number;
  documentName: string;
};

export const metaData: PaginationType = {
  start: 1,
  totalItems: 540,
  documentName: "Valley Sales Strategy",
};
